string firstName = 'Vitap'; //we are creating a new variable and assining a value to it.
string lastname = 'Ramdevputra';
string fullName = firstName + ' ' + lastname;

system.debug('full name is ---> ' + fullName);

firstName = 'Taha';
lastname = 'Admin';
fullName = firstName + ' ' + lastname;
system.debug('NOW full name is ---> ' + fullName);