string s = '';

//s is blank --> string is BLANK.
//s is NOT blank --> value of s.

if(string.isBlank(s)){
     system.debug('String is BLANK. Come on.');
}
else {
    system.debug(s);
}


if(s == ''){
    system.debug('String is still BLANK. Come onnnn.');
} else { 
    system.debug(s);
}
